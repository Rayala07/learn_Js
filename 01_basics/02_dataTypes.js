"use strict"; //Treats whole JS code as newer ver.

/*
; this is avoided to increase Readabillity , you can use it also (optional). 
*/

let name = "Me" //String
let age = 22 //Integer
let isLogged = true //boolean

//number => 2 to power 53 range of the number
//bigint (stockmarket for largest numbers)
//null => standalone value / representing empty / object type it is.
//undefined => when any value is not defined to any var. / it is also type - undefined type
//symbol => unique

//to know typeOf the Data present..
console.log(typeof "", 2, null);
