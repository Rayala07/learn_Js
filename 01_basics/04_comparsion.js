
/*
<
> 
<=
>=
These all are common and easily understandable , they return Boolean result 

*/

console.log("2" == 2) //=> will return "True" bcz == converts the dataType if not same and returns the from the value only.

console.log("3" === 3) //=> will return "False" bcz === compares the 'Value' & 'DataType' both before returning the result.